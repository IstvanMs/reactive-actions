
source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in reactive-actions.gemspec
gemspec

# Start debugger with binding.pry
gem "pry-rails", group: [:development, :test]